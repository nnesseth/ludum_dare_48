using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DoorExterior : MonoBehaviour
{/*
     private bool doorIsClosed = true;
    private bool playerAtDoor = false;
    private int minDistance = 5;

    void Start()
    {
        // Get player location ** no longer needed **
        // playerAtDoor = GameObject.Find("Player").transform;
    }

    // Update is called once per frame
    void Update()
    {
        // if player is close to the door, then allow player to cast a ray...    handle in Interactable.cs
        // if raycast hits door...      handle in interactionRay.cs and Interactable.cs
            // if door is closed, then cue open animation
            // else keep the door shut

    } 
*/}
